@model ItemPedidoModel
@{
    ViewBag.Subtitulo = "Exclusão De Item de Pedido";
}

<h1 class="text-primary">@ViewBag.Subtitulo</h1>
<hr>
<h4>Deseja realmente excluir o item de pedido <span class="text-danger"><b>@Model.Produto.Nome</b></span>?</h4>

<form asp-action="Excluir" class="w-25">
    <input type="hidden" asp-for="@Model.IdPedido">
    <input type="hidden" asp-for="@Model.IdProduto">
    <div class="mt-3">
        <button class="btn btn-danger" type="submit">Sim</button>
        <a asp-action="Index" asp-route-ped="@Model.IdPedido" class="btn btn-success">Não</a>
    </div>
</form>